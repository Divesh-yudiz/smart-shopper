const width = 1920;
const height = 1080;
const commonFont = 'Arial';
const config = Object.freeze({
    width: width,
    height: height,
    centerX: width / 2,
    centerY: height / 2,
    title: 'Game',
    version: 1.0,
    fonts: {
        text: commonFont
    },
});

export default config;